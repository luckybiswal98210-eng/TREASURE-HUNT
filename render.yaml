services:
  - type: web
    name: campus-hunt-form
    runtime: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: STORAGE_ROOT
        value: /var/data
    disk:
      name: campus-hunt-storage
      mountPath: /var/data
      sizeGB: 1
